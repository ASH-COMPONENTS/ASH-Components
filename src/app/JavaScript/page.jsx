import CodeBoard from "../components/codeBoard/codeBoard"

const page = () => {
  return (
    <div className='JS' style={{paddingTop: '60px'}}>
        <CodeBoard />
    </div>
  )
}

export default page